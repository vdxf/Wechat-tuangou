<!--components/nav-list/nav-list.wxml-->
<view class="nav-box">
    <view class="nav-item {{currentIndex === index ? 'active' : ''}}" data-index="{{index}}" data-item="{{item}}"  bind:tap="handleChangeNav" wx:for="{{buyGroupList}}" wx:key="Id" wx:for-item="item" wx:for-index="index">
        <text>{{item.Name}}</text>
    </view>
</view>